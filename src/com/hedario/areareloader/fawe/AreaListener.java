package com.hedario.areareloader.fawe;

import org.bukkit.event.Listener;

public class AreaListener implements Listener {
	AreaReloader plugin;

	public AreaListener(AreaReloader plugin) {
		this.plugin = plugin;
	}
}